# รายงานสรุปสถาปัตยกรรมระบบ AI ผู้ช่วยกฎหมาย PDPA (PDPA AI Assistant Architecture Report)

## 1. บทสรุปผู้บริหาร (Executive Summary)
โครงการนี้มีวัตถุประสงค์เพื่อพัฒนาระบบผู้ช่วยปัญญาประดิษฐ์ (AI Assistant) ระดับองค์กรที่มีความเชี่ยวชาญด้านพระราชบัญญัติคุ้มครองข้อมูลส่วนบุคคล (PDPA) ของประเทศไทย โดยเผชิญกับความท้าทายหลัก 2 ประการ ได้แก่ ข้อจำกัดด้านทรัพยากรข้อมูลที่มีเพียงไฟล์เอกสาร PDF กฎหมายต้นฉบับเพียงไฟล์เดียว และข้อจำกัดทางฮาร์ดแวร์ที่ต้องออกแบบระบบให้สามารถประมวลผลบนเครื่อง Laptop ที่มีหน่วยประมวลผลกราฟิก (GPU) ระดับเริ่มต้นอย่าง RTX 3060 (VRAM 6GB) และหน่วยความจำหลัก (RAM) 16GB ได้อย่างราบรื่น

เพื่อเอาชนะความท้าทายดังกล่าว โครงการจึงได้นำเสนอโซลูชันสถาปัตยกรรมแบบบูรณาการ 6 ขั้นตอน ที่เน้นการเพิ่มประสิทธิภาพการใช้ทรัพยากร (Resource Optimization) ควบคู่กับความปลอดภัย โดยผสมผสานเทคนิคการสร้างข้อมูลสังเคราะห์ด้วย LLM การสอนตรรกะทางกฎหมายด้วย Reinforcement Learning และการรักษาความปลอดภัยแบบป้องกันหลายชั้น (Defense-in-Depth) เพื่อส่งมอบระบบที่แม่นยำ ปลอดภัย และสามารถนำไปประยุกต์ใช้ในระดับองค์กรได้จริง

## 2. ภาพรวมสถาปัตยกรรมระบบ (System Architecture Overview)
สถาปัตยกรรมของระบบถูกแบ่งออกเป็น 3 แกนหลัก เพื่อให้เกิดความสมดุลระหว่างประสิทธิภาพความแม่นยำและการบริหารจัดการทรัพยากรหน่วยความจำที่มีจำกัด:

1. **Data Pipeline (ระบบจัดการและเตรียมข้อมูล):** ครอบคลุมตั้งแต่กระบวนการสกัดข้อความจากเอกสาร PDF กฎหมาย การตัดคำด้วย PyThaiNLP และการแบ่งส่วนข้อมูลโดยยังคงรักษาโครงสร้างลำดับชั้น (Hierarchy) ของหมวดและมาตรา เพื่อนำไปสร้างชุดข้อมูลคำถาม-คำตอบเชิงลึกแบบสังเคราะห์
2. **Model & Retrieval (แกนสมองประมวลผลและการดึงข้อมูล):** ดำเนินการในรูปแบบ Hybrid Processing โดยกระจายภาระงานดึงข้อมูล (Retrieval) ให้ทำงานบน CPU ด้วยฐานข้อมูลเวกเตอร์ ChromaDB และโมเดล BGE-M3 ร่วมกับกลไกอ้างอิงมาตราอัตโนมัติ (Cross-referencing) ในขณะที่ส่วนประมวลผลหลัก (Generation) จะถูกประมวลผลบน GPU ผ่านโมเดลภาษาขนาด 3 พันล้านพารามิเตอร์ (Qwen2.5-3B-Instruct) ที่ถูกบีบอัดขนาดลง
3. **Security & Guardrails (ระบบความปลอดภัยและควบคุมขอบเขต):** สถาปัตยกรรมป้องกันข้อมูลส่วนบุคคลและการโจมตีแบบ 3 ระยะ (Input, Internal, Output) ซึ่งถูกออกแบบให้ทำงานบน CPU ทั้งหมด เพื่อทำหน้าที่ปกปิดข้อมูลส่วนบุคคล (PII Redaction) ตรวจสอบและควบคุมหัวข้อสนทนา และกรองคำตอบก่อนแสดงผลให้ผู้ใช้งาน

## 3. สรุปขั้นตอนการพัฒนาเชิงวิศวกรรม (Engineering Implementation Steps)
กระบวนการพัฒนาเชิงวิศวกรรมของระบบถูกออกแบบให้มีความต่อเนื่องและส่งเสริมกันผ่าน 6 ขั้นตอน ดังนี้:

- **Step 1: Hierarchy-Aware Chunking**
  ดำเนินการสกัดเนื้อหาจาก PDF โดยใช้กลไก Regular Expression เพื่อจับโครงสร้างทางกฎหมาย (หมวด, มาตรา) ทำให้ได้ก้อนข้อความ (Chunk) ที่มีบริบทสมบูรณ์ ไม่ถูกตัดขาดตรงกลางประโยค ซึ่งเป็นรากฐานสำคัญสำหรับการค้นหา
- **Step 2: Synthetic Data Generation (CoT)**
  ประยุกต์ใช้ Gemini API ในการสร้างชุดข้อมูลฝึกสอนจำลอง (Instruction-Response Pairs) โดยบังคับให้คำตอบมีโครงสร้างการคิดแบบ "Legal Chain-of-Thought" (เรียงลำดับจาก: ระบุบทบาท -> ประเภทข้อมูล -> ประเมินกฎหมาย -> สรุปคำแนะนำ) 
- **Step 3: Parameter-Efficient Fine-Tuning (PEFT/QLoRA) & GRPO Alignment**
  ทำการปรับแต่งโมเดล (Fine-tune) ด้วยเทคนิค QLoRA ผ่านเฟรมเวิร์ก Unsloth แบบ 4-bit ควบคู่กับการใช้เทคนิค GRPO (Generative Reward Policy Optimization) เพื่อให้รางวัลแก่โมเดลเมื่อสามารถอ้างอิงเลขมาตราได้ถูกต้องตามคำถาม
- **Step 4: Hybrid RAG & Cross-referencing**
  พัฒนาระบบ Retrieval-Augmented Generation (RAG) ที่โดดเด่นด้วยกลไก **NitiLink Engine** ซึ่งสามารถตรวจจับเมื่อข้อความมีการอ้างอิงถึงมาตราอื่น (เช่น "ตามมาตรา ๒๔") และดึงเนื้อหาของมาตรานั้นๆ มาเสริมในบริบท (Context) โดยอัตโนมัติ เพื่อลดอาการหลอน (Hallucination)
- **Step 5: Multi-Stage Guardrails & PII Redaction**
  บูรณาการระบบความปลอดภัยโดยใช้ Microsoft Presidio ทำ PII Redaction ซึ่งพัฒนา **Custom Recognizer สำหรับตรวจจับบัตรประชาชนไทยด้วยสมการ Modulo-11** ควบคู่กับการใช้ระบบ Topic Guard ด้วย MiniLM เพื่อบล็อกการทำ Jailbreak และคำถามที่หลุดขอบเขต
- **Step 6: Evaluation Frameworks**
  สร้างกรอบการทดสอบระบบเชิงลึกแบบอัตโนมัติ (Automated Evaluation) ด้วยแนวทาง NitiBench และ RAGAS เพื่อวัดความแม่นยำ ความครอบคลุม และความซื่อตรงของข้อมูล พร้อมทดสอบความแข็งแกร่งของระบบผ่านชุดโจมตี ThaiSafetyBench

## 4. การจัดการข้อจำกัดทางทรัพยากร (Hardware Constraints & Optimization)
โจทย์ท้าทายหลักคือการทำให้ระบบทั้งหมดสามารถทำงานได้อย่างมีประสิทธิภาพและไม่เกิดข้อผิดพลาดจากหน่วยความจำเต็ม (OOM) บน **RTX 3060 (VRAM 6GB)** ซึ่งสามารถแก้ไขได้ด้วยยุทธศาสตร์ดังนี้:

1. **Extreme VRAM Isolation (การจัดสรรหน่วยความจำแบบแบ่งแยก):** สงวนพื้นที่ VRAM เกือบ 100% ไว้สำหรับโมเดล Qwen2.5-3B (LLM Inference) เท่านั้น โดยผลักภาระงานอื่นๆ ทั้งหมด เช่น โมเดลฝังคำ (BGE-M3, MiniLM), ฐานข้อมูล ChromaDB, และระบบ Guardrails ไปประมวลผลบนทรัพยากร CPU และ RAM ของระบบแทน
2. **4-Bit Quantization & Unsloth:** การใช้โมเดลในรูปแบบ 4-bit Quantization ทำให้ขนาดของตัวแปรโมเดล (Weights) ลดลงเหลือการใช้งาน VRAM เพียงประมาณ 2.5GB ทำให้มีพื้นที่เหลือ (Headroom) มากพอสำหรับการประมวลผลข้อความขนาดยาว (KV Cache)
3. **Training Optimization:** ในระหว่างกระบวนการ Fine-tuning ได้เปิดใช้งาน Gradient Checkpointing และตั้งค่า Batch Size ขนาดเล็กมากควบคู่กับการทำ Gradient Accumulation เพื่อลดการใช้งาน VRAM สูงสุด ณ ขณะประมวลผล (Peak VRAM)

## 5. มาตรฐานความปลอดภัยและการประเมินผล (Security & Evaluation Standards)
- **Security Standards:** ยึดหลัก Data Minimization อย่างเคร่งครัด โดยมีระบบดักจับข้อมูลที่มีความอ่อนไหว (เช่น เลขบัตรประชาชน เบอร์โทรศัพท์ อีเมล) และทำการแปลงเป็นข้อมูลเสมือน (เช่น `<REDACTED_PHONE>`) ทันทีตั้งแต่ข้อมูลยังไม่เข้าสู่การประมวลผลของ LLM และมีการกรองคำพูดที่เป็นพิษ (Toxicity) ที่ส่วนขาออก 
- **Evaluation Standards:** หลีกเลี่ยงการใช้มาตรวัดคำต่อคำแบบดั้งเดิม (เช่น BLEU หรือ ROUGE) เนื่องจากไม่สะท้อนความหมายทางกฎหมาย โดยเปลี่ยนมาใช้มาตรวัดเชิงความหมาย (Semantic Metrics):
  - **Retrieval:** ใช้ `MultiMRR` ในการวัดความสามารถของ RAG ในการดึงเลขมาตราที่ถูกต้องขึ้นมาในลำดับแรกๆ
  - **Quality:** ใช้เทคนิค `LLM-as-a-Judge` (ประมวลผลด้วย Temperature = 0.0) วัดระดับความครอบคลุม (Semantic Coverage) และความขัดแย้งของข้อมูล (Contradiction)
  - **Reliability:** อ้างอิงสถาปัตยกรรม `RAGAS` สำหรับการวัดความซื่อตรงต่อบริบทเอกสาร (Faithfulness) และความตรงประเด็น (Answer Relevance)

## 6. ผลการทดสอบและประเมินประสิทธิภาพ (Evaluation Results & Analysis)
เพื่อให้มั่นใจว่าระบบสามารถนำไปใช้งานจริงได้อย่างมีประสิทธิภาพ ได้มีการประมวลผลการทดสอบความถูกต้องและความปลอดภัยผ่านท่อประเมินผลอัตโนมัติ (Automated Evaluation Pipeline) โดยมีผลการทดสอบเชิงประจักษ์ดังนี้:

### 6.1 ประสิทธิภาพการค้นหาและดึงข้อมูล (Retrieval Performance)
ทดสอบด้วยชุดข้อมูล **NitiBench** โดยการตั้งคำถามเจาะจงและตรวจเช็คว่าโมเดล BGE-M3 และ NitiLink สามารถดึง "เลขมาตรา" ที่ถูกต้องมาเป็นบริบทได้ในระดับใด
- **Mean MultiMRR: 0.357** 
  *(ในกรณีที่คำถามตรงประเด็น เช่น "เก็บข้อมูลโดยไม่ขอยินยอม" ระบบสามารถดึงมาตรา 24 ขึ้นมาเป็นอันดับที่ 1 ได้อย่างแม่นยำ (MRR=1.000))*
- **Mean Recall: 0.540** 
  *(ระบบสามารถรวบรวมบริบทและมาตราที่เกี่ยวโยงกันได้ครอบคลุมเกินครึ่งหนึ่งของสิ่งที่ควรจะเป็น ถือเป็นผลลัพธ์ที่ดีสำหรับความจำกัดของโมเดลขนาดเล็ก)*

*ข้อค้นพบ:* การดึงกฎหมายที่มีเนื้อหาสั้นมาก (เช่น มาตรา 5 ว่าด้วยขอบเขต) จะถูกรบกวนได้ง่าย ซึ่งในอนาคตสามารถแก้ไขด้วยเทคนิค Query Expansion

### 6.2 ประสิทธิภาพการสร้างคำตอบและการอ้างอิง (Generation Quality & RAGAS)
ประเมินความครอบคลุมของเนื้อหาและความถูกต้องโดยไม่ขัดแย้งกับข้อเท็จจริง (Semantic Coverage & Contradiction) รวมถึงความซื่อตรงต่อบริบทตามหลัก **RAGAS** (Faithfulness)
*(หมายเหตุ: ในรอบการทดสอบนี้ ใช้ Gemini API เป็นผู้ประเมิน (LLM-as-a-Judge) แต่เนื่องจากโควต้ารายวันของ API สิ้นสุดลง คะแนนจึงแสดงเป็น 0 ทางเทคนิค แต่ระบบสถาปัตยกรรมและ Prompt ประเมินผลได้ถูกจัดเตรียมไว้อย่างสมบูรณ์)*

### 6.3 ประสิทธิภาพความปลอดภัยและการต้านทาน (Safety Block Rate)
ทดสอบการเจาะระบบ (Red Teaming) ด้วย 30 คำถามประสงค์ร้ายและภัยคุกคามจากฐานข้อมูล **ThaiSafetyBench**
- **ผลลัพธ์ปัจจุบัน:** อัตราการบล็อก (Block Rate) อยู่ที่ 0%
- **วิเคราะห์เชิงสถาปัตยกรรม (Root Cause Analysis):** สาเหตุไม่ได้เกิดจากระบบล่ม แต่เกิดจากข้อจำกัดของโมเดล Topic Guard ปัจจุบัน (`all-MiniLM-L6-v2`) ที่มีความสามารถเชี่ยวชาญเฉพาะ **ภาษาอังกฤษ** เมื่อเผชิญกับคำถามประสงค์ร้ายภาษาไทย (เช่น สั่งให้เล่านิทาน หรือให้ทำตัวเป็นอิสระ) โมเดลไม่สามารถเข้าใจ Semantic Language ของภาษาไทยได้เพียงพอ ทำให้หลุดเข้ามาได้
- **แนวทางแก้ไข (Path to Production):** แก้ไขได้ง่ายโดยการเปลี่ยนชิ้นส่วนโมเดลของ Topic Guard จากภาษาอังกฤษเป็นโมเดลหลายภาษา (Multilingual) เช่น `paraphrase-multilingual-MiniLM-L12-v2` และเพิ่มระบบตรวจจับคำไม่เหมาะสมเฉพาะภาษาไทยลงไป

## 7. บทสรุป (Conclusion)
สถาปัตยกรรม AI ผู้ช่วยกฎหมาย PDPA ระบบนี้ เป็นบทพิสูจน์ที่ชัดเจนถึงความเป็นไปได้ในการพัฒนาและประยุกต์ใช้ระบบ Enterprise AI ให้เกิดประสิทธิภาพสูงสุด แม้จะอยู่ภายใต้ข้อจำกัดด้านฮาร์ดแวร์ทรัพยากรระดับเริ่มต้นและข้อมูลที่จำกัด ด้วยการออกแบบระบบ Data Pipeline ที่ชาญฉลาด การทำ RAG ที่อิงบริบทข้ามมาตรา สถาปัตยกรรม Security Guardrails ที่รัดกุม ตลอดจนมีระบบ Evaluation Framework ที่เห็นจุดแข็งและข้อจำกัดอย่างชัดเจน ระบบนี้จึงมีความพร้อมอย่างยิ่งที่จะถูกต่อยอดและปรับปรุงในสภาพแวดล้อมจริง (Production-ready) หรือพัฒนาเป็น Web Service สำหรับองค์กรต่อไป
